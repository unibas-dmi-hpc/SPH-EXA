#pragma once

// just the project source for now to locate the input HDF5 file
// can be extended to include git revision etc as CMake replacement for
// the Makefile based stuff inside the grackle repository
#define PROJECT_SOURCE_DIR "@PROJECT_SOURCE_DIR@"