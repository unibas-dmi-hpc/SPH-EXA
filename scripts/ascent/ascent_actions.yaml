# Trigger actions defined in the associated file "binning_actions.yaml"
#
# Written by Jean M. Favre
# Updated by Yiqing Zhu
# tested Tue Jun  4 22:00 CEST 2023
-
  action: "add_triggers"
  triggers:
    # t1:
    #   params:
    #     condition: "cycle() % 100 == 0"
    #     actions_file: "binning_actions.yaml"
    t2:
      params:
        condition: "cycle() % 1 == 0"
        actions_file: "rank_color.yaml"